<select {{on "change" (fn this.updatePermission @permission @flag @updateDisableState)}}>
  {{#each @options as |optionObject|}}
  <option value={{optionObject.value}} selected={{eq @value optionObject.value}}>{{optionObject.label}}</option>
  {{/each}}
</select>