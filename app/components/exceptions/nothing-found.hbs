<div class="no-content" style={{html-safe (concat 'font-style: ' @fontStyle)}}>

  {{#if this.infoIconRequired}}
    <i class="fa fa-info-circle"></i>
  {{/if}}
  <div class="description">{{@description}}</div>
</div>