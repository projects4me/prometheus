{{#bootstrap-modal
  title="view.app.wiki.tag.create"
  confirmLabel="global.form.save"
  closeLabel="global.form.cancel"
  confirm="save"
  close="removeModal"}}
  <form {{action 'confirm' on='submit' target=view}}>
    <div class="form-group">
      <label>Name</label>
      {{input class="form-control" value=name}}
    </div>
  </form>
{{/bootstrap-modal}}
