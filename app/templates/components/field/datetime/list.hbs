{{!-- List template for datetime field--}}
<span>
  {{#if definition.link}}
    <a href="javascript:void(0);" {{action navigate}}>{{format-date model.field}}</a>
  {{else}}
    {{format-date model.field}}
  {{/if}}
</span>
