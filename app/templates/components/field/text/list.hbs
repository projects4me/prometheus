<!-- List template for text input field-->
<span>
  {{#if definition.link}}
    <a href="javascript:void(0);" {{action navigate}}>{{get-prop model field}}</a>
  {{else}}
    {{get-prop model field}}
  {{/if}}
</span>
