<div class={{concat "modal fade " @containerClass}}>
  <div class="modal-dialog">
    <div class="modal-content box box-primary">
      <div class="modal-header">
        <AppUi::Button type="button" class="close" data-dismiss="modal" aria-hidden="true"><i class="fa fa-times" aria-hidden="true"></i></AppUi::Button>
        <h4 class="modal-title">{{t this.title}}</h4>
      </div>
      <div class="modal-body">
        {{yield}}
      </div>
      <div class="modal-footer">`
        {{!-- close is handled in .js of component, so better to pass noop--}}
        <AppUi::Button type="button"  data-btn="cancel" @onClick={{fn (noop)}} class="btn btn-default" data-dismiss="modal">{{t this.closeLabel}}</AppUi::Button>
        {{#if (and this.confirmLabel (not @removeConfirmBtn))}}
          <AppUi::Button type="button" data-btn={{if @dataSaveBtn @dataSaveBtn 'save'}} disabled={{@disableSaveBtn}} class="btn btn-primary" @onClick={{this.confirm}}>{{t this.confirmLabel}}</AppUi::Button>
        {{/if}}
      </div>
    </div>
  </div>
</div>
