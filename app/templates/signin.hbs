<div class="login-box">
  <div class="login-logo">
    <a href="#"><img alt="{{t "application"}}" src="/img/app/logo-256.png"><div class="clearfix"></div>{{safe-html (t "views.signin.title")}}</a>
  </div>
  {{!-- /.login-logo --}}
  <div class="login-box-body">
    {{#if this.errorMessage}}
      <p class="login-box-msg">{{this.errorMessage}}</p>
    {{else}}
      <p class="login-box-msg">{{t "views.signin.message"}}</p>
    {{/if}}

    <form {{action 'authenticate' on='submit'}} method="post">
      <div class="form-group has-feedback">
        <Input id='username' class='form-control' placeholder={{t "views.signin.username"}} @value={{this.username}}/>
        <span class="glyphicon glyphicon-user form-control-feedback"></span>
      </div>
      <div class="form-group has-feedback">
        <Input id='password' class='form-control' @type='password' placeholder={{t "views.signin.password"}} @value={{this.password}}/>
        <span class="glyphicon glyphicon-lock form-control-feedback"></span>
      </div>
      <div class="row">

        <div class="col-xs-12">
          <button type="submit" class="btn btn-primary btn-block btn-flat">{{t "global.form.signin"}}</button>
        </div>

        <div class="col-xs-4 col-xs-offset-4">
          <div class="checkbox icheck">
            <label>
              <input type="checkbox">{{t "views.signin.remember"}}
            </label>
          </div>
        </div>

      </div>
    </form>

    <div class="social-auth-links text-center">
      <p>{{t "views.signin.or"}}</p>
      <a href="#" class="btn btn-block btn-social btn-facebook btn-flat"><i class="fa fa-facebook"></i>{{t "views.signin.facebook"}}</a>
      <a href="#" class="btn btn-block btn-social btn-google btn-flat"><i class="fa fa-google-plus"></i>{{t "views.signin.google"}}</a>
    </div>
    {{!-- /.social-auth-links --}}

    <a href="#">{{t "views.signin.forgot"}}</a><br>
  </div>
  {{!-- /.login-box-body --}}
</div>
